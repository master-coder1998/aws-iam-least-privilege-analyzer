# Runtime
boto3>=1.34.0
botocore>=1.34.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.12.0
moto[iam,s3,dynamodb,secretsmanager,sts,organizations]>=5.0.0

# Linting & formatting
ruff>=0.3.0
mypy>=1.9.0
boto3-stubs[iam,s3,dynamodb,organizations,securityhub,cloudtrail,accessanalyzer,scheduler,sns,sqs,kms]

# Security scanning (CI)
bandit>=1.7.8
safety>=3.0.0
